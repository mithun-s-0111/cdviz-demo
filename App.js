// SonarQube Issue Example
let unusedVariable = "This variable is never used"; // Unused variable issue
var globalVar = 42; // Global variable issue
const password = "hardcodedPassword123"; // Hardcoded credentials

function duplicateCode() {
  let x = 1;
  let y = 2;
  let z = 3;
  return x + y + z;
}

function anotherDuplicate() {
  let x = 1;
  let y = 2;
  let z = 3;
  return x + y + z;
}
